# Tracker type
tracker_type: bytetrack

# Tracking parameters
track_high_thresh: 0.4
track_low_thresh: 0.1
new_track_thresh: 0.5
track_buffer: 60
match_thresh: 0.8
fuse_score: True

# Detection settings  
min_box_area: 10
mot20: False
